CREATE TABLE IF NOT EXISTS `zora-retention-stack.zora_prod.follow_events`
(
  follower_wallet   STRING     NOT NULL,
  creator_wallet    STRING,
  follow_date       TIMESTAMP,
  is_active         BOOL
)
OPTIONS (
  description = "Social graph — wallet follows creator wallet."
);
